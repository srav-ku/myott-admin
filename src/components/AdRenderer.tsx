export default function AdRenderer({ position }: { position: string }) {
  // Ads are disabled in the private vault refactor.
  // Return null to ensure zero intrusive ads are shown.
  return null;
}
